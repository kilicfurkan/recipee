import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  input: {
    height: 40, 
    width: 200,
    backgroundColor: '#f1f1f1',
    borderRadius: 5,
    marginBottom: 10,
    paddingLeft: 5,
  }
});
